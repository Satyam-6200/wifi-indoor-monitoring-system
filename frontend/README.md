# PulseWiFi frontend

The React/Vite dashboard for the indoor monitoring MVP.

```bash
npm ci
npm run dev
npm run build
npm run lint
```

Telemetry is deliberately illustrative. See the root README and `docs/data-flow.md` before connecting an authorized collector.
